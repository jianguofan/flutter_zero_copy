schema: spec-driven

# Project context (optional)
# This is shown to AI when creating artifacts.
context: |
  Tech stack: Flutter/Dart, C++ (OpenGL/Metal/D3D11), CMake
  We use conventional commits
  Domain: cross-platform GPU zero-copy texture sharing (macOS/Windows/Linux)
  Related:
    - Lava App enterprise Flutter architecture review (see specs/lava-architecture-review/)
    - Lava App implementation spec (see specs/lava-implementation/)
    - Verified SDK: lava-device-controll (LAN/WAN connection strategies)

# Per-artifact rules (optional)
rules:
  proposal:
    - Keep proposals under 500 words
    - Always include a "Non-goals" section
  tasks:
    - Break tasks into chunks of max 2 hours
